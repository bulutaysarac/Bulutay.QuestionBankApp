@model List<CategoryListModel>
@{
    Layout = "_AdminLayout";
}

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Tanım</th>
            <th>İşlem</th>
        </tr>
    </thead>
    <tbody>
        @if (Model?.Count > 0)
        {
            @foreach (var category in Model)
            {
                <tr>
                    <td>@category.Id</td>
                    <td>@category.Definition</td>
                    <td>
                        <a class="btn btn-warning" asp-action="Update" asp-controller="Category" asp-route-id="@category.Id">Güncelle</a>
                        <a class="btn btn-danger" asp-action="Remove" asp-controller="Category" asp-route-id="@category.Id">Sil</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
<a class="btn btn-primary d-block" type="button" asp-action="Create" asp-controller="Category">Oluştur</a>